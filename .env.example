# No environment variables are required for the public no-login template.
#
# Data is saved in each visitor's own browser via localStorage.
# Add auth/database/email variables later only if the app becomes account-based.
